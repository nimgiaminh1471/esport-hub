{
  "name": "esports-hub",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Slack bot + trang realtime cho esports (LoL, VALORANT)",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "smoke": "node --env-file-if-exists=.env src/providers/lol.js --smoke",
    "notify": "node --env-file-if-exists=.env src/notify.js",
    "notify:dry": "DRY_RUN=1 node --env-file-if-exists=.env src/notify.js",
    "pnl": "node --env-file-if-exists=worker/.dev.vars src/pnl.js",
    "serve": "python3 -m http.server 8080 --directory docs",
    "smoke:val": "node --env-file-if-exists=.env src/providers/valorant.js --smoke"
  }
}
